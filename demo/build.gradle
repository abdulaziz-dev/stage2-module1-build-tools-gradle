group = 'com.epam'
version = '2.1'
repositories {
    mavenCentral()
    mavenLocal()
}

dependencies {
    implementation project(path: ':utils')
}

test {
    useJUnitPlatform()
}

compileJava {}.dependsOn(':utils:publishToMavenLocal')